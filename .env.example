# Optional. The committed BMDL snapshot is sufficient for normal continuation.
# These variables are needed only when an authorized read-only refresh is required.
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
BMDL_SCHEMA=bmdl

