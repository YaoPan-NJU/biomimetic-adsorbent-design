# portfolio_100 状态说明

本目录由远端 `GLM` 分支提交 `f7f797c` 选择性接收进入 `main`，包含 BPA、ROX、PFOA、PFBS 和2,6-DCP各20套探索方案、组内淘汰记录及原分支全局排序。

这些文件是**探索档案**，不是 `main` 的活动评分表或最终候选清单。原因包括：

- 多数方案只有邻近先例或 `tier none` 核心机制；
- 部分方案把生物来源主体、毒性靶点或通用化学机制误计为仿生硬对应；
- 多项分数早于 `main` 对 BPA非对称锚、ROX-2、BPA-5和术语边界的后续裁决；
- 部分框架假设与当前“一项硬对应、动态可选、蛋白/肽默认排除”合同不一致。

使用顺序：先读 `docs/BIOMIMETIC_DESIGN_FRAMEWORK.md` 和 `SUPERVISOR_AUDIT.md`，再把本目录原始方案作为假设来源。不得继承 `RANKING.md` 的分数、晋级身份或下一动作。

历史文件中的无修饰“受体”是分支原文。新的用户交付必须按 `docs/TERMINOLOGY.md` 改写为人工识别单元、生物原型或准确生物类别。
