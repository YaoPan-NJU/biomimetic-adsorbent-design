# ROX-2 监督裁决与动态转配

隔离 Reviewer 将 ROX-2 复核为 `40/100`、`terminate`，并把降级后的非印迹极性孔道限定为大环内酯类别 `control_only`。监督代理接受该处置。

Reviewer 建议下一步返回 BPA-1，但该建议基于隔离输入，未包含随后已经完成的 BPA-1 与 BPA-2 深审结果。BPA-1 现为 `70/revise/experiment-gated`，BPA-2E 与 PFBS-1 已为 `control_only`，BPA-2R 与 PFBS-2 均无确切受体。因此不重复分配 BPA-1；依据潘尧授权的动态转配规则，下一纸面候选改为尚未深审的 BPA-5 双功能化杯芳烃网络。

该改配不改变 ROX-2 的终止裁决，也不删除 Reviewer 的原始建议。BPA-5 必须先通过水相离散受体门，未通过不得材料化。
