# 跨设备接手指南(2026-07-19 检查点)

> 本文件供 Pan Yao 或任一会话在新设备上接手时使用。全部进度已在 GitHub 上,无本地独有文件。

## 一、今天发生了什么(按时间序)

1. **PFBS-CD-GAC-v2 评估**:64 个隔离 agent 六维评估 + 对抗核验。裁定:创新性低、Kd≥10⁴ L/kg 在 β-CD 单齿化学下结构性不可达(100% CD 天花板约 2.2×10³ L/kg)、仿生框架 2/7、评审预测约 61/100。
2. **终止裁决**:四条优化路径全部证伪,建议终止方案、保留问题。设计会话(framework-correction)已独立执行终止并记录于 PROJECT_STATE.yaml。
3. **准入门禁规范**:按 Pan Yao 规则"创新性低即在流程中迭代掉"起草四道准入门(证据锚点/创新性下限/定量上限/框架分类),`orchestration/ADMISSION_GATE_SPEC_DRAFT.md`,待批准生效。
4. **首次门禁执行**:R1a 抗污界面 GAC 概念在门 2 被杀(Cai 2017 JCIS 机制先验,对抗核验维持原判);门 3 退回修复(记录留存);门 4 通过(仿生启发)。门禁记录 `orchestration/gates/G16_PFBS_AFI_GAC_ADMISSION.yaml`。
5. **广泛侦察与假想组合**:四路文献侦察(约 200 次检索,约 120 条题录经 Crossref/PubMed 核实)。七个假想裁定完毕,详见 `rounds/shortchain_redirection_1/HYPOTHESIS_PORTFOLIO.md`。最强候选:**概念 α = H1 脱水选择性阴离子识别 × H4 孔口动力学门闩**(分级介孔碳 + 孔口脱水选择性门闩 + 腔内分配 + 动力学囚禁抗置换)。
6. **设计会话并行进展**(framework-correction 分支):ROX pH 响应设计 v2.0 完成(FcRn 原型),PROJECT_STATE 记录 phase=rox_ph_responsive_design_completed、status=ready_for_experimental_validation、next_action=ROX Phase 0 实验验证;框架文档新增浓度情景分析组件。

## 二、进度在 GitHub 上的位置

| 分支 | 内容 | 状态 |
|------|------|------|
| `origin/GLM` (b60d8d8) | 设计会话最新检查点:ROX pH 响应设计 v2.0 完成、PFBS-CD-GAC 终止记录、框架文档新增浓度情景分析、更新后的 PROJECT_STATE.yaml;含我的评估与终止备忘录副本 | 设计会话已推送并并入 GLM |
| `origin/worktree-framework-correction` (b60d8d8) | 与 GLM 同点 | 已同步 |
| `origin/worktree-shortchain-redirection` (PR #2 → GLM,草案) | 我的检查点包:六维评估、终止备忘录、准入门禁规范草案、R1a 门检全套(G1-G4+对抗核验+G16)、假想组合、本指南 | 已推送,PR 待审 |

PR 地址:https://github.com/YaoPan-NJU/biomimetic-adsorbent-design/pull/2

## 三、明天如何接手

1. 克隆或拉取仓库,`git fetch origin`。
2. **阅读顺序**:`docs/BIOMIMETIC_DESIGN_FRAMEWORK.md`(GLM 最新版)→ `PROJECT_STATE.yaml`(GLM)→ 本指南 → `rounds/shortchain_redirection_1/HYPOTHESIS_PORTFOLIO.md`(PR 分支)。
3. **待 Pan Yao 裁定的三件事**:
   - (a) 概念 α 是否形成候选概念简报并执行四道准入门(纯文书,约 1 小时);
   - (b) 准入门禁规范是否批准生效(批准后在 `orchestration/PROTOCOL.md` 登记);
   - (c) PR #2 是否合并入 GLM(GLM 已含设计会话的 ROX 完成态与 PFBS-CD-GAC 终止记录;本 PR 叠加我的检查点包,两分支重复的评估/终止文件内容相同,合并无冲突)。
4. **若 α 获准**:按 `orchestration/ADMISSION_GATE_SPEC_DRAFT.md` 执行 G1-G4,产出 `rounds/<scheme>_admission/` 与 G17 门禁决定;α 的四个预注册杀伤条件见假想组合第 5 节(选择性窗口外推不足 10² 即迭代;长链 PFSA 冲击置换即终止;门闩化学水相强度不足即终止;DOM 小级分主导则降级)。
5. **ROX 线**(design 会话拥有):按其 PROJECT_STATE 的 next_action 执行 Phase 0 实验验证(注意:实验执行属周佳奇,仓库内只做文书)。

## 四、遗留与 reconciliation 警告

1. **framework-correction worktree 内仍有未跟踪的旧副本**:`orchestration/ADMISSION_GATE_SPEC_DRAFT.md`、`orchestration/gates/G13_PFBS_AFI_GAC_ADMISSION.yaml`(旧编号,与仓库既有 G13_BPA2 冲突)、`rounds/pfbs_afi_admission/`。**这些是我写入的早期副本,规范版本已在 PR #2(门决定已改号 G16)。不要在该 worktree 提交它们;应删除,以 PR #2 为准。**
2. **重复的评估/终止文件**:两分支各含 `PFBS_CD_GAC_V2_EVALUATION.md` 与 `..._TERMINATION_RECOMMENDATION.md`,内容相同,合并无冲突。
3. **BMDL 本地导出**(framework-correction worktree 的 `data/bmdl_local/` 与 `export_bmdl_data.py`,未跟踪):属设计会话工作;CSV 可从远程只读库重新导出,非不可替代决策;该会话自行决定是否提交。
4. **v2 评估发现的两条文献勘误**,设计会话应注意:(i) v2 文档引用的"三项 ITC 研究"(Xiao 2019/Wang 2020/Gao 2021)经多路检索无法定位,Weiss-Errico 2017 被错误表征为 γ-CD 研究(真实文献方法为 ¹⁹F NMR/HOESY 且报告 β-CD 结合最强);(ii) v2 引用的"Nature Water 2025 氟化 PAF"经 Crossref 全刊检索未能核实,真实锚点为 Yang 2024 Nat. Commun. 与 Kasula 2026 CEJ。
5. ROX 设计文档是本分支 PFBS v2 评估中 §10 模板残留的源头(逐字对应已记录于评估第 4 节 C-1),属设计会话内部文档管理事项。

## 五、边界声明

今天全部工作为文书工作;未授权任何实验、采购或合成;文献题录经 Crossref/PubMed 核实,内容级结论标注待全文核验;无 SciFinder 权限;凭证扫描通过(181 个跟踪文件)。
